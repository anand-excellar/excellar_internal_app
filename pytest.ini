[pytest]
DJANGO_SETTINGS_MODULE = portal.settings_test
# Everything: the NAV suite carried over from navdash, the CallSpread parity and
# dashboard tests, and the portal's own auth/gateway tests.
testpaths = tests navfund/tests callspread/tests gateway portal
python_files = test_*.py tests.py
asyncio_mode = auto
