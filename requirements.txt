# One project, one requirements file.

django>=5.0
djangorestframework>=3.15
python-dotenv>=1.0
requests>=2.31
pyyaml>=6.0
dj-database-url>=2.0

# NAV: scheduling, exchange access, on-chain reads, Google Sheets export
huey>=2.5
redis>=5.0
ccxt>=4.0
aiohttp>=3.9
google-api-python-client>=2.0
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.2
tzdata>=2024.1

# Postgres driver — only needed when DATABASE_URL points at Postgres, which is
# the production setup. Harmless to install alongside the SQLite default.
psycopg2-binary>=2.9